# --- Konfigurasi Database PostgreSQL ---
# Username database (biasanya 'postgres' secara default)
DB_USER=postgres

# Password database Anda (ganti dengan password yang Anda buat saat instalasi PostgreSQL)
DB_PASSWORD=dk8515vw

# Host database (biasanya 'localhost' untuk development lokal)
DB_HOST=localhost

# Port database (5432 adalah default PostgreSQL)
DB_PORT=5432

# Nama database yang Anda gunakan (ganti dengan nama DB yang Anda buat, misal 'wedding_db')
DB_NAME=wedding_planner_db

# --- Konfigurasi Server ---
# Port di mana server backend akan berjalan
PORT=3000
```

JWT_SECRET=super_secret_wedding_planner_key

EMAIL_USER=moneymood.id@gmail.com
EMAIL_PASS=vsmopowxklskpmbn
ADMIN_EMAIL=moneymood.id@gmail.com

FRONTEND_URL=http://localhost:4200 #nanti ganti menggunakan domain jika sudah deploy contoh: https://wedding-planner.moneymood.id