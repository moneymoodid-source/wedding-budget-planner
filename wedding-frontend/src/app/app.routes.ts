import { Routes } from '@angular/router';

// Kita biarkan kosong dulu karena navigasi Login vs Dashboard 
// ditangani langsung di dalam app.component.ts menggunakan @if
export const routes: Routes = [];